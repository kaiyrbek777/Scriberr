services:
  scriberr:
    build:
      context: .
      dockerfile: Dockerfile
    image: scriberr:local
    ports:
      - "8080:8080"
    volumes:
      - scriberr_data:/app/data
    restart: unless-stopped

volumes:
  scriberr_data:

